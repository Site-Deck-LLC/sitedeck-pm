http:
  routers:
    sitedeck-pm-ops:
      rule: Host(`ops.sitedeck.pro`)
      service: sitedeck-pm
      entryPoints:
        - websecure
      tls:
        certResolver: letsencrypt

  services:
    sitedeck-pm:
      loadBalancer:
        servers:
          - url: http://172.16.0.1:3000
